var postState = "<%= @post.sold? ? 'Đã' : 'Chưa' %>" + " bán";
var contentHelper = 
"Đánh dấu là " + "<%= @post.sold? ? 'chưa' : 'đã' %>" + " bán";

$('.dropdown-button').dropdown('close');
$('td.state').text(postState);

var hihgLight = $('td.state').parent()
// Highlight on clicking
hihgLight.toggleClass('teal');

// Revert color
setTimeout(function(){
  hihgLight.toggleClass('teal')
}, 200); 

// Update contentHelper
$('#post-action li a.mark').text(contentHelper);
